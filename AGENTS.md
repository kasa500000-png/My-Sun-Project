# My-Sun-Project 작업 지침

## 제품 정체성

이 저장소의 공식 제품명은 **마이썬 운동일지**입니다. 운동 기록, 부위별 자극 분석, 식단 기록과 개인 목표 관리를 지원하는 모바일 우선 PWA입니다. 다른 프로젝트의 제품명이나 도메인 모델을 임의로 이식하지 않습니다.

## 안전 경계

- 인증, Supabase RLS, API 요청·응답 형태, 운동 볼륨 계산, 식단 영양 계산, 기존 사용자 데이터 구조를 UI 작업과 함께 변경하지 않습니다.
- 운영 데이터는 읽기 중심으로 취급하며 테스트를 위해 삭제하거나 변조하지 않습니다.
- 비밀키, 테스트 계정, 개인 정보는 로그·문서·스크린샷에 남기지 않습니다.
- 데이터 계약 변경이 불가피하면 하위 호환성과 롤백 경로를 먼저 문서화합니다.

## UI·UX 원칙

- 기존 아이보리·웜그레이·차분한 녹색 계열과 기록 중심의 브랜드 인상을 유지합니다.
- 모바일 360px부터 데스크톱 1440px까지 콘텐츠 잘림과 가로 스크롤이 없어야 합니다.
- 터치 대상은 최소 44×44px, 핵심 입력은 48px 이상을 유지합니다.
- 상태는 색상만으로 전달하지 않고 텍스트, 아이콘, 부호를 함께 사용합니다.
- 키보드 포커스, 스크린리더 이름, 오류 연결, 모달 복귀를 기본 품질로 취급합니다.
- 화면별 하드코딩보다 `app/globals.css`의 의미 기반 토큰과 공통 클래스를 우선합니다.
- 기능을 제거하거나 오류를 숨기는 방식으로 UI 문제를 해결하지 않습니다.

## 검증 명령

```bash
npm run typecheck
npm run lint
npm test
npm run quality
npm run build
npm run validate
```

환경 또는 의존성 때문에 실행하지 못한 검증은 성공으로 기록하지 않고 차단 사유를 남깁니다.

## 작업 산출물

- 감사 및 설계 문서: `docs/uiux/`
- 단계 로그: `.logs/uiux/`
- 화면 증거: `artifacts/uiux/`
- UI·UX 테스트: `tests/e2e/uiux/`, `tests/visual/uiux/`, `tests/uiux-*.test.ts`

각 단계 로그에는 대상, 발견 문제, 변경 파일, 검증 결과, 남은 위험과 커밋을 기록합니다.
